input:
  uses: http.receiver
steps:

  - uses: map
    with:
      expression:
        id : data.id
        name : data.name
      language: jmespath

  - uses: relational.write
    with:
      connection: postgresql
      schema: public
      table: data
