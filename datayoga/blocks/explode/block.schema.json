{
  "description": "Split a field into multiple records",
  "title": "Explode",
  "type": "object",
  "properties": {
    "field": {
      "description": "Field",
      "type": "string"
    },
    "target_field": {
      "description": "Target field to add to the result. If omitted, the source field name will be used",
      "type": "string"
    },
    "delimiter": {
      "description": "Delimiter to use for splitting the source field",
      "type": "string",
      "default": ","
    }
  },
  "additionalProperties": false,
  "required": ["field"],
  "examples": [
    {
      "field": "features",
      "target_field": "feature",
      "delimiter": ";"
    }
  ]
}
