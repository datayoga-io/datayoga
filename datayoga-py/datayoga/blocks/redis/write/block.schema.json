{
  "title": "redis.write",
  "description": "Write to a Redis data structure",
  "type": "object",
  "properties": {
    "connection": { "description": "Connection name", "type": "string" },
    "command": {
      "enum": ["HSET", "SADD", "XADD", "RPUSH", "LPUSH", "SET", "ZADD"],
      "type": "string",
      "title": "Redis command",
      "description": "Redis command"
    },
    "key_field": {
      "type": "string",
      "title": "Key field",
      "description": "Field to use as the Redis key"
    }
  },
  "additionalProperties": false,
  "required": ["connection", "key_field", "command"]
}
