{
  "title": "Job",
  "description": "Job descriptor",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "uses": {
        "description": "Block type",
        "type": "string"
      },
      "with": {
        "description": "Properties",
        "type": ["object", "array"]
      }
    },
    "additionalProperties": false,
    "required": ["uses", "with"]
  }
}
