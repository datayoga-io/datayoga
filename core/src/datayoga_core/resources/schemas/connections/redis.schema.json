{
  "description": "Redis",
  "properties": {
    "type": {
      "description": "DB type",
      "type": "string",
      "enum": ["redis"]
    },
    "host": { "description": "Redis DB host", "type": "string" },
    "port": {
      "description": "Redis DB port",
      "type": "integer",
      "minimum": 1,
      "maximum": 65535
    },
    "username": {
      "description": "Redis DB username",
      "type": "string"
    },
    "password": {
      "description": "Redis DB password",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": ["type", "host", "port"],
  "dependentSchemas": {
    "username": {
      "required": ["password"]
    }
  },
  "examples": [
    {
      "cache": {
        "type": "redis",
        "host": "localhost",
        "port": 6379
      }
    }
  ]
}
